# Opd-token-allocation-system
Backend OPD token allocation system built using FastAPI that manages fixed doctor time slots with priority-based scheduling and dynamic patient reallocation for real-life hospital scenarios.
